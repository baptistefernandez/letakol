export const environment = {
  production: false,
  // debouncer default time
  debouncerTimeout: 100,
  // Loading display time
  transitionTimeout: 500,
  // Time to wait promises
  promiseTimeout: 10000,
};
